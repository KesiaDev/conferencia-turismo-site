{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm --filter @conferencia/web build && pnpm --filter @conferencia/api build && ./apps/api/install-chromium.sh"
  },
  "deploy": {
    "startCommand": "cd apps/api && node dist/index.js",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 3
  }
}
