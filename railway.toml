[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm --filter @conferencia/api build"

[deploy]
startCommand = "cd apps/api && npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

# API Configuration - Railway
# API URL: conferenciaapi-production.up.railway.app
# Status: ACTIVE - Serving JSON API endpoints
# Date: 2024-01-XX
# FIXED: API now serves correct JSON responses