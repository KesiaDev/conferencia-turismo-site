[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm --filter @conferencia/web build"

[deploy]
startCommand = "cd apps/web && npx serve dist -p $PORT -s"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

# Frontend Configuration - Railway
# Site URL: turismocinematografico.com.br
# Status: ACTIVE - Serving frontend with new footer
# Date: 2025-01-XX
# FIXED: Frontend now serves with company logos in footer