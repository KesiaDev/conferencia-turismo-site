[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm --filter @conferencia/web build"

[deploy]
startCommand = "cd apps/web && npx vite preview --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
